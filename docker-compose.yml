version: "3"
services:
  app:
    image: decidim
    build:
      context: .
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - PORT=3000
      - DATABASE_HOST=pg
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=p28ZRFZeitqovv9Puvse
      - RAILS_ENV=development
      - SECRET_KEY_BASE=J9jEZgGFudNB4kEeqYHwoht4IQvinBhf
      - FROM_MAILER_ADDRESS=me@domain.org
      - APPLICATION_NAME=My application name
    ports:
      - 3000:3000
    links:
      - pg

  pg:
    image: postgres
    volumes:
      - pg-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=p28ZRFZeitqovv9Puvse
      - POSTGRES_USER=postgres
      - POSTGRES_DB=decidim_app_development

volumes:
  node_modules: {}
  pg-data: {}
